package javabasics.assignment1.packages;

public class ContentOfPackages {
public static void main(String[] args) {
    /*
            package name must in lowercase subpackages are separated by .(dot)
            com.techouts.javabasics.cj5
            com is the main package
            techouts is subpackage of com
            javabasics is subpackage in

            packages are very helpful in managing the class form Naming conflictions.

           access Modifiers
           Public ---> It ha a Global access form any where we can call the methods and access the classes and methods.
                        we can use
                            with in class
                            with in package sub classes
                            with in package on sub classes
                            out side of class in same package
                            out side the packages subclasses
                            out side the package non sub classes
           default----> it is only in same package range
                           we can access the classes and methods in only the same packages it self
                           with in class
                            with in package sub classes
          protected---> it is used in same package and subclasses in other packages
                           with in class
                           with in package sub classes
                           with in package on sub classes
                           out side the packages subclasses
           private ---->it has only with in the class itself
     */
    }
}
